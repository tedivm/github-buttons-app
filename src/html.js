const aComment = '<!-- Place this tag where you want the button to render. -->'
const scriptComment = '<!-- Place this tag in your head or just before your close body tag. -->'
const scriptElement = '<script async defer src="https://buttons.github.io/buttons.js"></script>'

export {
  aComment,
  scriptComment,
  scriptElement
}
